method: 'grid'
name: 'DMS-IPA_outputlayer'
metric:
  goal: 'maximize'
  name: 'pearson_correlation'  # this works because we only use a single validation set
parameters:
  dms_output_layer_type:
    values:
      - "mlp"
      - "regression"
      - "regression_sigmoid"
      - "bias_only"
      - "identity"
