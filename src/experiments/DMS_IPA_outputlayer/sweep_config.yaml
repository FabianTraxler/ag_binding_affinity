method: 'grid'
name: 'DMS-IPA_outputlayer_2regressionheadlayers'
metric:
  goal: 'maximize'
  name: 'pearson_correlation'  # this works because we only use a single validation set
parameters:
  dms_output_layer_type:
    values:
      - "mlp"
      - "regression_sigmoid"
  seed:
    values:
      - 1
      - 2
      - 3
      - 4
      - 5
